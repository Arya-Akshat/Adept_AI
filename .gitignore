# Environment Variables
.env
backend/.env
flask/.env
.env.local
.env.development.local
.env.test.local
.env.production.local
credentials.json
flask/credentials.json
token.json
flask/token.json

# Dependencies
node_modules/
.venv/
.venv_new/
venv/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class

# Node.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build
dist/
build/
dist-ssr/

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Application Data (Local)
backend/src/constants/rawData/*
!backend/src/constants/rawData/.keep
backend/src/constants/processedData/*
!backend/src/constants/processedData/.keep
# We might want to keep metadata structure but not data? 
# For now, let's ignore the json if it contains local paths, but user edited it manually.
# Actually, pdfs.json contains metadata about local files. It should probably be ignored if we want a clean repo.
backend/src/constants/metadata/pdfs.json

# Logs
logs
*.log